[package]
name = "swiftlib"
version = "0.1.0"
edition = "2021"

[dependencies]

[features]
default = ["library"]
library = []
std-support = []  # When used from std-based binaries; disables the custom panic_handler

[lib]
name = "swiftlib"
path = "lib.rs"
test = false
bench = false