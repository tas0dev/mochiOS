[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[dependencies]
swiftlib = { path = "../../user", features = ["std-support"] }

[profile.release]
panic = "abort"
lto = true

[profile.dev]
panic = "abort"
